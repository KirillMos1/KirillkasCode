print("KirillkasCode v.0.0.1_alpha for x64")
while True:
	com = input(">> ")
	com_list = com.split(" ")
	if com_list[0] = "print":
		com_list.remove(0)
		returned = ""
		for arg in com_list:
			returned += agr + " "
		print(returned)
	else:
		print("I can`t do this!")
